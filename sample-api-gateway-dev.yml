zuul:
  routes:
    workspaces-api:
      path: /workspaces/**
      service-id: workspaces-api
    employees-api:
      path: /employees/**
      service-id: employees-api
  ignored-services:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8282/eureka

server:
  port: 9094
